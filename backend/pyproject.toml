[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "url_shortener"
requires-python = ">=3.13.2"
version = "0.1.0"
dependencies = [
    "fastapi",
    "motor"
]
[tool.setuptools.packages.find]
where = ["src"]